function assert(x) {
    if (x) {
        putchar(46);
    } else {
        putchar(70);
    }
}

function main() {
    assert(true);
    assert(false);
    assert(!undefined);
    assert(null);
    return 0;
}